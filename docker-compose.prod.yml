version: "3"
services:
  api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
    environment:  
      - MONGO_URL=mongodb://db:27017/diary
      - NODE_ENV=production
    links:
      - db
  db:
    image: mongo:3.4
    volumes:
      - './mongodb_data:/data/db'
