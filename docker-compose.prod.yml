version: "3"
services:
  api:
    image: mydiary:1.0
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
    environment:  
      - NODE_ENV=production
    links:
      - db
    restart: always
  db:
    image: mongo:3.4
    volumes:
      - './mongodb_data:/data/db'
